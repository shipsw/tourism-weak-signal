# 密钥与本地配置
.env

# 输出产物：日志不入库；数据与日报提交回仓库（供 GitHub Actions 持久化并部署 Pages）
outputs/logs/
# 运行时状态文件（每次运行变化，不入库）
outputs/data/serpapi_usage.json
outputs/data/serpapi_status.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/

# GitHub Pages 构建产物（由 CI 生成）
_site/
